---
title: "浏览器里怎么查词？Shift 查词是什么？"
description: "按住 Shift 把鼠标移到词上就弹词典——这是 Yomitan 那套习惯，Fushi 的浏览器扩展也是这样。app 内的阅读器和本地视频点词即查；内置网页播放器的 Fushi 字幕 Shift 和点击都行；其他网页只有 Shift。附扩展安装步骤。"
category: "浏览器扩展"
order: 110
date: 2026-09-20
---

## 先说「Shift 查词」

在 Fushi 的阅读器和视频里查词，是**点**一下那个词。但在浏览器网页上，点击会触发链接、按钮、选中文字，没法拿来查词。所以浏览器查词工具（[Yomitan](https://yomitan.wiki/)、以前的 Yomichan）用的是另一套习惯：

> **按住 Shift，把鼠标移到词上**，词典就弹出来；松开 Shift、移开鼠标，弹窗关掉。不用点、不用选。

用过 Yomitan 的人对这个动作有肌肉记忆；没用过的第一次会以为扩展装坏了——它在等你按 Shift。

Fushi 里三种地方，触发方式不一样：

| 在哪 | 怎么查 |
|---|---|
| app 内阅读器、本地视频 / 番剧字幕、漫画 | **点击**词（桌面还可以在设置里开「悬停即查词」，鼠标停上去就查） |
| 内置网页播放器里的 **Fushi 字幕** | **Shift 悬停** 和 **点击** 都行 |
| 浏览器扩展（任意网页） | 只有 **Shift 悬停** |

## 浏览器扩展：在任意网页查词

Fushi 自带一个浏览器扩展，让你在任何网页上用 app 里的词典查词、制卡。桌面专用（Windows / macOS），Chrome / Edge 这类 Chromium 浏览器；手机浏览器装不了扩展，手机上直接在 app 内读。

### 安装

**设置 → 浏览器扩展**，跟着页面上的步骤走：

1. 点 **准备扩展文件**。Fushi 会自动开启查词服务（Yomitan API 服务器），把扩展解压到本机，并把文件夹路径复制到剪贴板。
2. 在浏览器地址栏打开 `chrome://extensions`（Edge 是 `edge://extensions`）。
3. 打开右上角的 **开发者模式**。
4. 点 **加载已解压的扩展程序**，粘贴刚才复制的路径，选那个文件夹。
5. 回 Fushi 点 **检测连接**，显示「插件已连接」就好了。扩展不需要再配任何东西，连接信息已经自动写好。

不放心的话点 **打开试用网页**：Fushi 自己提供的一张网页，右上角点扩展图标应该弹面板，按住 Shift 移到练习句的词上应该弹词典。

### 使用

- **按住 Shift，鼠标移到词上** → 弹出和 app 里一样的词典弹窗（同一套词典、同一个排版）。
- 弹窗里点 **加号** 就制卡，整句会自动带上，卡进你连的 Anki。
- 释义里还有不认识的词，继续 Shift 移上去，嵌套查。
- 弹窗大小在 **设置 → 查词** 里的「扩展弹窗最大宽度 / 高度」调，可以和 app 内弹窗分开设。

### 装不上 / 没反应

- **「查词服务未开启」**：设置 → 浏览器扩展里先开「Yomitan API 服务器」，或者再点一次「准备扩展文件」。
- **端口 19633 被占用**：多半是你之前装的 Yomitan 拉起的 yomitan-api 组件（一个 Python 进程）。Fushi 会提示，可以直接结束它，或在 Yomitan 高级设置里关掉 Yomitan API 再重开 Fushi 的服务器。
- **「浏览器中加载的扩展不是最新版本」**：Fushi 更新后扩展文件也更新了，去 `chrome://extensions` 点那个扩展的「重新加载」。
- **某些网页取不到词**：文字是画在 canvas 上或者嵌在图片里的页面（一些漫画站、PDF 预览）扩展没法取字，属正常。
- Fushi 关了扩展也就断了——它查的是本机 Fushi 的词典。

## 内置网页播放器：看网页视频

流媒体、B 站、YouTube 这类网页视频，不用下载也能在 Fushi 里边看边查：**视频 → 导入**，粘贴网页地址（不是直链），会用内置网页播放器打开。

- **播放模式**：**内置**（1080p，可超分、可截图制卡）或 **原生窗口**（4K、硬件 DRM 的站点用这个；制卡先排队，之后「切到内置模式制作排队卡片」一次做完）。
- **字幕轨**：Fushi 实时采集页面上的字幕，渲染成自己的一层——这一层就是上表说的「Fushi 字幕」，**Shift 悬停和点击都能查词**。开 **隐藏站点字幕**，就只剩 Fushi 这层。
- 站点本身没字幕、或者字幕是画在画面里的，采集不到，那就只剩 Shift 查网页上别的文字。

## 已经在用 Yomitan？

两边可以共存，也可以只留一边：

- **继续用 Yomitan 查网页**没问题，Fushi 的扩展只是多一个选择；两者都用 Shift，装在一起会各弹各的，建议网页查词只留一个。
- 想让 **GameSentenceMiner 之类走 yomitan-api 协议的工具** 改查 Fushi 的词典：开 Fushi 的 Yomitan API 服务器（端口 19633，可设密钥），把工具指向它。
- Textractor / LunaTranslator / mpv 这类抓文本的工具，可以直接把文本推给 Fushi 的 texthooker（设置里连它们的 WebSocket），在 Fushi 里逐词点查、制卡，不再需要 texthooker 网页 + Yomitan 那一套。

## 浏览器之外

- **Windows 全局查词**：在任何应用里拖选一个词，按快捷键（设置 → 快捷键 → 全局），查词卡片在鼠标旁弹出。
- **Android 全局查词**：在其他应用里长按选中文字，文字菜单里点 Fushi（没有就走分享），弹窗盖在原应用上。
